# WOMS 排程規則定義

---

## 常數

| 常數名稱 | 值 | 說明 |
|---|---|---|
| `DAILY_CAPACITY` | 10,000 | 單一工廠每日最大產能（片） |
| `MAX_ORDER_QUANTITY` | 2,500 | 單張訂單數量上限（片） |
| `MIN_ORDER_QUANTITY` | 25 | 單張訂單數量下限（片） |
| `MAX_LOOKAHEAD_DAYS` | 90 | 往後查找可用產能的最大天數 |

---

## 訂單狀態

| 狀態 | 說明 |
|---|---|
| `PENDING` | 訂單已建立，尚未完成排程。可能是剛建立還未跑排程，也可能是修改或取消觸發全局重排後暫時回到此狀態。全局重排時，這些訂單會被優先嘗試排入。 |
| `SCHEDULED` | 所有 `ProductionSlot` 都已建立，`remaining_quantity = 0`，訂單有明確的生產計畫。`lastSlotDate` 在此狀態下才有意義。此狀態下可能 `isDelayed = true`（排好了但會延誤）或 `isDelayed = false`（排好且準時）。 |
| `IN_PRODUCTION` | 第一個 `ProductionSlot` 的日期已到，工廠開始實際生產。此狀態下的取消會有額外確認警告。 |
| `COMPLETED` | 所有 `ProductionSlot` 的生產都已實際完成（交期已過），訂單自動判定為完成。用以區分「已排程但還沒生產」與「已生產完畢」。 |
| `CANCELLED` | 訂單已取消，對應的所有 `ProductionSlot` 釋放，觸發 `PENDING` 訂單重排。取消後不可恢復，若需重新接單須建立新訂單。 |

---

## 一、新增訂單排程邏輯

### 1. 數量不合法（超過上限 / 低於下限）

**情境：** 新訂單輸入 3,000 片（超過 2,500 上限），或輸入 10 片（低於 25 下限）

**系統行為：**
- 在 API validation 層直接拒絕，回傳錯誤訊息
- 不進入排程邏輯，訂單不建立

---

### 2. 交期已過（過期交期）

**情境：** 今天是 5/10，新訂單的客戶要求交期是 5/8（過去的日期）

**系統行為：**
- 不允許建立，回傳警告「交期已過，無法新增此訂單」

---

### 3. 新訂單超過單日產能上限 → 拆分跨天排程

**情境：** 今天已排了 A（3,000 片）、B（5,000 片）、C（1,500 片），共 9,500 片，剩餘 500 片。新訂單 D 要 800 片，當日剩餘空間不足以一次容納。

**系統行為：**
1. 從最早可排的日期開始，每天計算 `available = DAILY_CAPACITY - daily_used`
2. 取 `min(available, remaining_quantity)` 排入當天，產生一筆 `ProductionSlot` 紀錄
3. `remaining_quantity` 扣除後繼續往下一天，直到 `remaining_quantity = 0`
4. `lastSlotDate` 更新為最後一批完成的日期
5. 往後查找的上限為 `MAX_LOOKAHEAD_DAYS`（90 天）

**決策原則：** 往後找的條件是「剩餘產能 > 0」，最大化每天產能利用率，不跳過任何有剩餘的日期。

**決策點：** 若直接插入現有排程空位後，`lastSlotDate <= customerDueDate`，則**不觸發全局重排**，直接完成排程。

---

### 4. 交期前剩餘產能不足 → 觸發全局重排（重排後可在交期前完成）

**情境：** 今天 5/10，訂單 D 交期 5/13 需要 800 片。系統逐日填入後，`lastSlotDate > customerDueDate`（最後 100 片只能排到 5/14）。

**觸發條件：** `lastSlotDate > customerDueDate`，且若直接插入現有排程空位（不動其他訂單）仍會晚於交期。

**系統行為：**
1. 從接到訂單的當下起，到排程中最新的訂單，全部納入，重新排程
2. 重排依照 `customerDueDate` 優先（EDD）、相同交期則以 `createdAt` FIFO 排序後依序安插
3. 讓所有訂單達到最佳化排程狀態
4. 若重排後能在交期前完成 → 訂單狀態設為 `SCHEDULED`，`isDelayed = false`

> **注意：** 排程進行中會加 lock，確保同一時間只有一個全局重排在寫入，後續進來的排程請求排隊等待。

---

### 5. 全局重排後仍無法在交期前完成 → 顯示最早可達成交期，由使用者決定

**情境：** 全局重排後，所有訂單最佳化安排下，訂單 D 的 `lastSlotDate` 仍晚於 `customerDueDate`。

**系統行為：**
1. 計算「把能往後推的 slot 都往後推」之後，最早能完成的日期（延遲交期）
2. 排程結果**先寫入資料庫**（`isDelayed = true`，`delayDays = lastSlotDate - customerDueDate`，`delayReason = CAPACITY_FULL`）
3. 排程結果寫入後**立即解鎖**，讓後續訂單可以繼續排程（不因使用者思考而阻塞）
4. 前端跳出小視窗，顯示：
   - 最早可達成的延遲交期
   - 「確認」與「取消」按鈕
5. 使用者選擇：
   - **確認** → 保留此排程結果，訂單狀態維持 `SCHEDULED`（`isDelayed = true`）
   - **取消** → 將「取消訂單」加入排程佇列，等前面的排程任務完成後執行，釋放 slot 並觸發 `PENDING` 訂單重排

---

### 6. 90 天內總剩餘產能不足，全局重排也無解

**情境：** 訂單 D 需要 2,000 片，但往後 90 天的總剩餘產能只有 1,800 片，即使全局重排後仍無法湊足。

**系統行為：**
1. 同上（第 5 點），計算最早可達成時間，顯示延遲交期並讓使用者決定
2. 前端以醒目方式提示「90 天內總剩餘產能不足，目前最早可完成日為 X/X」
3. 使用者可選擇確認延遲交期，或取消訂單

---

### 7. 同交期多筆訂單的優先順序

**情境：** 同一天進來三筆訂單 E（2,000 片）、F（2,000 片）、G（2,000 片），交期都是 5/20，但 5/20 前只剩兩天空位。

**決策：** 以 `createdAt` 先後（FIFO）決定全局重排中的優先順序，`createdAt` 早的訂單優先安插。

---

## 二、修改訂單排程邏輯

### 1. 修改數量或交期，全局重排後可在交期前完成

**系統行為：**
1. 釋放該訂單原本佔用的所有 `ProductionSlot`（歸還產能）
2. 觸發全局重排，從頭以最佳化順序重新排程所有訂單
3. 修改後的訂單以新數量 / 新交期參與排程
4. 若能在新交期前完成 → 訂單狀態 `SCHEDULED`，`isDelayed = false`

> **實作注意：** 釋放舊 slot 與建立新 slot 需包在同一個 transaction，避免中間狀態。

---

### 2. 修改數量或交期，全局重排後無法在交期前完成

**系統行為：**
1. 釋放該訂單原本佔用的所有 `ProductionSlot`
2. 觸發全局重排
3. 重排後 `lastSlotDate > customerDueDate` → 同新增流程第 5 點：
   - 排程結果寫入（`isDelayed = true`）
   - 前端顯示最早可達成的延遲交期
   - 使用者選擇「確認」或「取消」

---

## 三、取消訂單邏輯

### 1. 一般取消

**系統行為：**
1. 釋放該訂單佔用的所有 `ProductionSlot`（歸還產能）
2. 訂單狀態設為 `CANCELLED`，不可恢復（若需重新接單，須建立新訂單）
3. 觸發全局重排，讓 `PENDING` 訂單有機會填入釋放的空位

---

### 2. 取消 `IN_PRODUCTION`（生產中）的訂單

**情境：** 訂單 N 狀態已是 `IN_PRODUCTION`，管理員要取消。

**系統行為：**
1. 允許取消，但前端跳出確認警告
2. 目前只生產一種晶片，已生產的量在取消後視為產能空位，可供其他訂單使用
3. 在 `Order` 上記錄 `cancelledFromStatus = IN_PRODUCTION` 供後續追蹤
4. 釋放所有 `ProductionSlot`，觸發全局重排

---

## 四、並發與資料一致性

### 1. 多個使用者同時新增訂單

**情境：** 5/15 剩 1,000 片，管理員甲填 800 片、管理員乙填 600 片，幾乎同時送出。

**解法：**
- 以按下送出的時間點記錄 `createdAt`，作為 FIFO 優先順序依據
- 排程過程加 lock（資料庫層 `SELECT ... FOR UPDATE` 或樂觀鎖 `version` 欄位）
- 確保同一時間只有一筆排程寫入成功，另一筆等前者完成後重新計算，可能被推到下一天

---

### 2. 使用者 A 修改訂單、使用者 B 同時查看同一筆訂單

**情境：** B 看到的排程日期是舊資料，但 A 已經改掉了。

**解法：**
- 前端查詢時帶上 `updatedAt` 時間戳
- 儲存前先比對，不一致就提示「有人剛剛更新了這筆訂單，請重新載入」，避免覆蓋

---

### 3. 使用者考慮延遲交期時，不阻塞後續排程

**情境：** 訂單 A 排程結果顯示延遲交期，使用者正在考慮是否接受，此時訂單 B、C 陸續進來。

**流程：**

| 步驟 | 動作 |
|---|---|
| 1 | 計算訂單 A 的全局排程，結果寫入資料庫（`isDelayed = true`），**立即解鎖** |
| 2 | 前端顯示訂單 A 的延遲交期結果與「確認 / 取消」按鈕（使用者慢慢考慮） |
| 3 | 訂單 B 進來 → 加入排程佇列，訂單 A 排程結束後立即開始計算 B 的全局排程 |
| 4 | 訂單 C 進來 → 加入排程佇列，等訂單 B 排完後計算 C 的全局排程 |
| 5 | 使用者決定取消訂單 A → 「取消訂單 A」加入排程佇列 |
| 6 | 等 B、C 排程計算完成後，執行「移除訂單 A」的全局重排，釋放空位 |

> **關鍵設計：** 排程結果寫入後即解鎖，使用者的考慮時間不影響後續訂單的排程流程。

---

## 五、全局重排規則

- 收集所有 `PENDING` 和 `SCHEDULED` 訂單，`IN_PRODUCTION` 的訂單不移動
- 釋放所有 `SCHEDULED` 訂單的 `ProductionSlot`（這些訂單回到 `PENDING`）
- 依 EDD（Earliest Due Date）優先、相同交期則 FIFO（`createdAt` 早的優先）排序後逐一拆分排入
- 排程進行中加 lock，確保同一時間只有一個全局重排在寫入

---

## 六、延誤判斷規則

```
isDelayed  = (lastSlotDate > customerDueDate)
delayDays  = lastSlotDate - customerDueDate  （單位：天）
```

**`delayReason` 枚舉：**

| 值 | 說明 |
|---|---|
| `CAPACITY_FULL` | 產能不足導致無法在交期前完成 |
| `MANUAL_DATE_UNREACHABLE` | 手動指定完成日往前回推後產能仍不足 |

---

## 七、拆分排程規則

```
available = DAILY_CAPACITY - daily_used
每天排入量 = min(available, remaining_quantity)
```

- 訂單支援跨天拆分，只要當天有剩餘產能（`available > 0`）就填入
- `lastSlotDate` 為最後一批完成的日期，是延誤判斷的唯一基準
- 往後查找上限為 `MAX_LOOKAHEAD_DAYS`（90 天）

---

## 八、排程優先順序規則

- 同交期的多筆訂單，以 `createdAt` 早的優先（FIFO）
- 全局重排時，所有 `PENDING` 和 `SCHEDULED` 訂單依 **EDD + FIFO** 排序後重新分配

---

## 九、過期與無效訂單規則

- 交期已過的訂單不允許建立，顯示「交期已過，無法新增此訂單」
- 數量超過上限或低於下限，在 API validation 層直接拒絕，不進入排程邏輯
