apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging
resources:
  - ../../base
secretGenerator:
  - name: woms-secret
    envs:
      - secret.env
generatorOptions:
  disableNameSuffixHash: true
